# Automatically created by: scrapy startproject
#
# For more information about the [deploy] section see:
# https://scrapyd.readthedocs.io/en/latest/deploy.html

# 指定 Scrapy 项目运行时读取哪个 settings 模块。
[settings]
# 默认配置模块是 house_info.settings。
default = house_info.settings

# ScrapyD 部署配置段，当前只保留项目名称。
[deploy]
# ScrapyD 服务地址示例，取消注释后可以部署到对应服务。
#url = http://localhost:6800/
# 部署时使用的项目名称。
project = house_info
